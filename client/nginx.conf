events {}
http {
   server {
      listen ${HOST}:${EXTERNAL_PORT};
      root /usr/share/nginx/html;
      
      location /api/ {
         proxy_pass http://api:${API_PORT}/;
      }

      location / {

      }

   }
}
